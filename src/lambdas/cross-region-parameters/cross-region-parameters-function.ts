// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for CrossRegionParametersFunction
 */
export interface CrossRegionParametersFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/lambdas/cross-region-parameters/cross-region-parameters.
 */
export class CrossRegionParametersFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: CrossRegionParametersFunctionProps) {
    super(scope, id, {
      description: 'src/lambdas/cross-region-parameters/cross-region-parameters.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/lambdas/cross-region-parameters/cross-region-parameters.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}